jwt:
  secret-key: ${JWT_SECRET_KEY}
jwt-token:
  access-token-expiration-seconds: ${ACCESS_TOKEN_EXPIRATION_SECONDS:172800}
  refresh-token-expiration-seconds: ${REFRESH_TOKEN_EXPIRATION_SECONDS:2592000}
