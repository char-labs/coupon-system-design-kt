interface:
  display_name: "Coupon Java 25 Validation"
  short_description: "Choose the smallest ./gradlew-first Java 25 validation command"
  default_prompt: "Use $coupon-java25-gradle-validation to pick the right format and validation commands for this coupon-system change, preferring plain ./gradlew when the launcher JVM is already Java 25."
