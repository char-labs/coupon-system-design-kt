# Legacy fallback for the load-test wrapper.
# Prefer the repository root `.env` / `.env.example` for shared local settings.

# Slack incoming webhook URL for load-test failure notifications.
LOAD_TEST_SLACK_WEBHOOK=

# Optional defaults for wrapper execution.
# LOAD_TEST_PROFILE=local
# LOAD_TEST_SLACK_NOTIFY_ON=always
