services:
  # k6 결과를 적재하는 시계열 저장소입니다.
  influxdb:
    image: influxdb:1.12.2
    container_name: coupon-k6-influxdb
    restart: unless-stopped
    environment:
      INFLUXDB_DB: myk6db
      INFLUXDB_HTTP_AUTH_ENABLED: "false"
      INFLUXDB_REPORTING_DISABLED: "true"
    ports:
      - "8086:8086"
    volumes:
      - k6_influxdb_data:/var/lib/influxdb

volumes:
  k6_influxdb_data:
